fun  factorial(n)
vars tmp
{       
    if (n == 0) then
    {
        return 1;
    }
    else
    {
        tmp = (n - 1);
        return (factorial(tmp) * n);
    }
}

fun  main(n)
{       
    return factorial(n);
}
