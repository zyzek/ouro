
fun  factorial(n)
vars cond, total
{       
    total = 1;

    while (n > 1) 
    {       
        total = (total * n);
        n = (n - 1);
    }
    
    return total;
}


fun main(n)
{       
    return factorial(n);
}
