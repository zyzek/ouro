fun  main(n)
vars tmp, pred, prepred
{       
    pred = 1;
    prepred = 0;

    while (n != 0)
    {
        tmp = pred;
        pred (+)= prepred;
        prepred = tmp;
        
        n (-)= 1;
    }

    return prepred;
}
