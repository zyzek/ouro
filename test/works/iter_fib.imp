
fun  fib(n)
vars tmp, pred, prepred
{       
    pred = 1;
    prepred = 0;

    while (n != 0)
    {
        tmp = pred;
        pred = (prepred + pred);
        prepred = tmp;
        
        n = (n - 1);
    }

    return prepred;
}


fun  main(n)
{       
    return  fib(n);
}
