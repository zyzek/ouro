#include "math"

fun  main(a, b, c, d, e)
vars tmp
{       
    tmp = max(a, b);
    a = min(a, b);
    b = tmp;

    tmp = max(b, c);
    b = min(b, c);
    c = tmp;

    tmp = max(c, d);
    c = min(c, d);
    d = tmp;

    tmp = max(d, e);
    d = min(d, e);
    e = tmp;


    tmp = max(a, b);
    a = min(a, b);
    b = tmp;

    tmp = max(b, c);
    b = min(b, c);
    c = tmp;

    tmp = max(c, d);
    c = min(c, d);
    d = tmp;


    tmp = max(a, b);
    a = min(a, b);
    b = max(a, tmp);

    tmp = max(b, c);
    b = min(b, c);
    c = tmp;


    tmp = max(a, b);
    a = min(a, b);
    b = tmp;


    print a;
    print b;
    print c;
    print d;

    return e;
}